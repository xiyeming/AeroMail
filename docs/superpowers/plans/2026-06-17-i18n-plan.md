# i18n 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 AeroMail 添加 English / 简体中文 国际化支持，统一前后端错误/状态本地化，并提供设置、Status Bar、Command Palette 三处语言切换入口。

**Architecture:** 前端使用 `vue-i18n` v10 懒加载 locale JSON，语言偏好持久化到 SQLite `settings.app.locale`；后端命令从 `Result<T, String>` 升级为 `Result<T, ErrorPayload>`，由前端根据错误码映射为本地化文本。

**Tech Stack:** Vue 3 + vue-i18n v10 + TypeScript + Pinia + Tauri v2 + Rust 2024 + rusqlite.

## Global Constraints

- Rust 2024 edition，`unsafe_code = "forbid"`，clippy `unwrap_used = "deny"`。
- 所有 Vue 组件使用 `<script setup lang="ts">`。
- 前端路径别名 `@` 指向 `src/`。
- 后端 Tauri Command 错误统一使用 `ErrorPayload { code, args }`。
- 新增文案必须同时写入 `en.json` 与 `zh-CN.json`，保持 key 一致。
- 每个 Task 结束时运行 `pnpm lint`、`pnpm type-check`、`cargo check`。

---

## Task 1: 安装 vue-i18n 并创建 i18n 模块

**Files:**
- Modify: `package.json`（安装依赖）
- Create: `src/i18n/index.ts`
- Create: `src/i18n/locales/en.json`
- Create: `src/i18n/locales/zh-CN.json`
- Modify: `src/main.ts`

**Interfaces:**
- Produces: `i18n` 实例，`Locale` 类型，`SUPPORTED_LOCALES`，`loadLocaleMessages(locale)`。

- [ ] **Step 1: 安装依赖**

```bash
cd /home/xiyeming/CodeSpaces/ToolsProjects/RutMail
pnpm add vue-i18n@10
```

- [ ] **Step 2: 创建 i18n 入口**

```typescript
// src/i18n/index.ts
import { createI18n } from 'vue-i18n';

export type Locale = 'en' | 'zh-CN';
export const SUPPORTED_LOCALES: Locale[] = ['en', 'zh-CN'];

export const i18n = createI18n({
  legacy: false,
  locale: 'en',
  fallbackLocale: 'en',
  messages: {},
  missingWarn: false,
  fallbackWarn: false,
});

export async function loadLocaleMessages(locale: Locale): Promise<void> {
  const messages = await import(`./locales/${locale}.json`);
  i18n.global.setLocaleMessage(locale, messages.default);
}
```

- [ ] **Step 3: 创建初始翻译文件**

```json
// src/i18n/locales/en.json
{
  "app": {
    "name": "AeroMail"
  },
  "nav": {
    "inbox": "Inbox",
    "accounts": "Accounts",
    "settings": "Settings"
  },
  "account": {
    "addAccount": "Add Account",
    "accountName": "Account Name",
    "provider": "Provider",
    "testConnection": "Test Connection",
    "delete": "Delete",
    "noAccounts": "No accounts yet"
  },
  "statusBar": {
    "online": "Online",
    "offline": "Offline",
    "lastSync": "Last sync: {time}",
    "syncComplete": "Sync complete",
    "syncing": "Syncing... {count}/{total} accounts"
  },
  "commandPalette": {
    "placeholder": "Search mail...",
    "switchLanguage": "Switch Language"
  },
  "settings": {
    "language": "Language",
    "english": "English",
    "simplifiedChinese": "简体中文"
  },
  "errors": {
    "IMAP_AUTH_FAILED": "IMAP authentication failed for {0}",
    "IMAP_CONNECTION_TIMEOUT": "IMAP connection timed out",
    "SMTP_SEND_FAILED": "Failed to send email: {0}",
    "INVALID_ACCOUNT_CONFIG": "Invalid account configuration: {0}",
    "DATABASE_ERROR": "Database error",
    "INTERNAL_ERROR": "Internal error: {0}",
    "UNKNOWN_ERROR": "An unknown error occurred"
  }
}
```

```json
// src/i18n/locales/zh-CN.json
{
  "app": {
    "name": "AeroMail"
  },
  "nav": {
    "inbox": "收件箱",
    "accounts": "账户",
    "settings": "设置"
  },
  "account": {
    "addAccount": "添加账户",
    "accountName": "账户名称",
    "provider": "厂商",
    "testConnection": "测试连接",
    "delete": "删除",
    "noAccounts": "暂无账户"
  },
  "statusBar": {
    "online": "在线",
    "offline": "离线",
    "lastSync": "最后同步：{time}",
    "syncComplete": "同步完成",
    "syncing": "同步中... {count}/{total} 账户"
  },
  "commandPalette": {
    "placeholder": "搜索邮件...",
    "switchLanguage": "切换语言"
  },
  "settings": {
    "language": "语言",
    "english": "English",
    "simplifiedChinese": "简体中文"
  },
  "errors": {
    "IMAP_AUTH_FAILED": "IMAP 认证失败：{0}",
    "IMAP_CONNECTION_TIMEOUT": "IMAP 连接超时",
    "SMTP_SEND_FAILED": "邮件发送失败：{0}",
    "INVALID_ACCOUNT_CONFIG": "账户配置无效：{0}",
    "DATABASE_ERROR": "数据库错误",
    "INTERNAL_ERROR": "内部错误：{0}",
    "UNKNOWN_ERROR": "发生未知错误"
  }
}
```

- [ ] **Step 4: 在 main.ts 注册 i18n**

```typescript
// src/main.ts
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import App from './App.vue';
import router from './router';
import { i18n } from './i18n';
import './styles/theme.css';
import './styles/fonts.css';

const app = createApp(App);
app.use(createPinia());
app.use(router);
app.use(i18n);
app.mount('#app');
```

- [ ] **Step 5: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 6: Commit**

```bash
git add package.json pnpm-lock.yaml src/i18n src/main.ts
git commit -m "feat(i18n): add vue-i18n and locale resource files" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: 统一后端错误模型 ErrorPayload

**Files:**
- Create: `src-tauri/src/models/error.rs`
- Modify: `src-tauri/src/models/mod.rs`
- Modify: `src-tauri/src/error.rs`
- Modify: `src-tauri/src/commands/account.rs`
- Modify: `src/composables/useTauriInvoke.ts`

**Interfaces:**
- Consumes: 现有 `AeroError` 变体。
- Produces: `ErrorPayload { code, args }`，`AeroError::to_payload()`，所有 Command 返回 `Result<T, ErrorPayload>`。

- [ ] **Step 1: 创建 ErrorPayload 模型**

```rust
// src-tauri/src/models/error.rs
use serde::Serialize;

#[derive(Debug, Clone, Serialize)]
pub struct ErrorPayload {
    pub code: String,
    pub args: Vec<String>,
}
```

- [ ] **Step 2: 注册模型模块**

```rust
// src-tauri/src/models/mod.rs
pub mod account;
pub mod error;
```

- [ ] **Step 3: 改造 AeroError**

```rust
// src-tauri/src/error.rs
use serde::Serialize;
use thiserror::Error;

use crate::models::error::ErrorPayload;

#[derive(Debug, Error, Serialize)]
#[serde(tag = "type", content = "message")]
pub enum AeroError {
    #[error("database error: {0}")]
    Database(String),
    #[error("account not found: {0}")]
    AccountNotFound(String),
    #[error("invalid account configuration: {0}")]
    InvalidConfig(String),
    #[error("connection test failed: {0}")]
    ConnectionTestFailed(String),
    #[error("internal error: {0}")]
    Internal(String),
}

impl AeroError {
    pub fn to_payload(&self) -> ErrorPayload {
        match self {
            AeroError::Database(_) => ErrorPayload {
                code: "DATABASE_ERROR".to_string(),
                args: vec![],
            },
            AeroError::AccountNotFound(id) => ErrorPayload {
                code: "ACCOUNT_NOT_FOUND".to_string(),
                args: vec![id.clone()],
            },
            AeroError::InvalidConfig(reason) => ErrorPayload {
                code: "INVALID_ACCOUNT_CONFIG".to_string(),
                args: vec![reason.clone()],
            },
            AeroError::ConnectionTestFailed(reason) => ErrorPayload {
                code: "CONNECTION_TEST_FAILED".to_string(),
                args: vec![reason.clone()],
            },
            AeroError::Internal(reason) => ErrorPayload {
                code: "INTERNAL_ERROR".to_string(),
                args: vec![reason.clone()],
            },
        }
    }
}

impl From<rusqlite::Error> for AeroError {
    fn from(err: rusqlite::Error) -> Self {
        AeroError::Database(err.to_string())
    }
}

impl From<serde_json::Error> for AeroError {
    fn from(err: serde_json::Error) -> Self {
        AeroError::Internal(err.to_string())
    }
}

impl From<AeroError> for ErrorPayload {
    fn from(err: AeroError) -> Self {
        err.to_payload()
    }
}
```

- [ ] **Step 4: 更新 account 命令返回 ErrorPayload**

```rust
// src-tauri/src/commands/account.rs
use tauri::State;

use crate::AppState;
use crate::models::account::{AccountConfig, AccountSummary};
use crate::models::error::ErrorPayload;

#[tauri::command]
pub async fn add_account(
    config: AccountConfig,
    state: State<'_, AppState>,
) -> Result<String, ErrorPayload> {
    let manager = state.account_manager.read().await;
    manager.add_account(config).map_err(AeroError::to_payload)
}

#[tauri::command]
pub async fn list_accounts(
    state: State<'_, AppState>,
) -> Result<Vec<AccountSummary>, ErrorPayload> {
    let manager = state.account_manager.read().await;
    manager.list_accounts().map_err(AeroError::to_payload)
}

#[tauri::command]
pub async fn delete_account(
    account_id: String,
    state: State<'_, AppState>,
) -> Result<(), ErrorPayload> {
    let manager = state.account_manager.read().await;
    manager.delete_account(&account_id).map_err(AeroError::to_payload)
}

#[tauri::command]
pub async fn test_account_connection(
    config: AccountConfig,
    state: State<'_, AppState>,
) -> Result<String, ErrorPayload> {
    let manager = state.account_manager.read().await;
    manager.test_connection(&config).await.map_err(AeroError::to_payload)
}
```

- [ ] **Step 5: 更新前端 invoke 封装以解析 ErrorPayload**

```typescript
// src/composables/useTauriInvoke.ts
import { invoke } from '@tauri-apps/api/core';
import { useI18n } from 'vue-i18n';

export interface ErrorPayload {
  code: string;
  args: string[];
}

export function useTauriInvoke() {
  const { t } = useI18n();

  async function call<T>(
    command: string,
    args?: Record<string, unknown>
  ): Promise<T> {
    try {
      return await invoke<T>(command, args);
    } catch (raw) {
      const payload = parseErrorPayload(raw);
      throw new Error(t(`errors.${payload.code}`, payload.args));
    }
  }

  return { call };
}

function parseErrorPayload(raw: unknown): ErrorPayload {
  if (typeof raw === 'string') {
    try {
      const parsed = JSON.parse(raw);
      if (parsed && typeof parsed.code === 'string') {
        return {
          code: parsed.code,
          args: Array.isArray(parsed.args) ? parsed.args : [],
        };
      }
    } catch {
      // fall through
    }
    return { code: 'UNKNOWN_ERROR', args: [raw] };
  }

  if (raw && typeof raw === 'object') {
    const obj = raw as Record<string, unknown>;
    return {
      code: typeof obj.code === 'string' ? obj.code : 'UNKNOWN_ERROR',
      args: Array.isArray(obj.args) ? (obj.args as string[]) : [],
    };
  }

  return { code: 'UNKNOWN_ERROR', args: [] };
}
```

- [ ] **Step 6: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/models/error.rs src-tauri/src/models/mod.rs src-tauri/src/error.rs src-tauri/src/commands/account.rs src/composables/useTauriInvoke.ts
git commit -m "refactor(error): introduce ErrorPayload for localized errors" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: 添加 settings 持久化命令

**Files:**
- Modify: `src-tauri/src/db/pool.rs`
- Create: `src-tauri/src/commands/settings.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs`

**Interfaces:**
- Produces: `set_setting(key, value)`, `get_setting(key)` commands，DB `set_setting` / `get_setting` 方法。

- [ ] **Step 1: 扩展 Database 提供 settings 读写**

```rust
// src-tauri/src/db/pool.rs
impl Database {
    // ... existing methods

    pub fn set_setting(&self, key: &str, value: &str) -> Result<(), AeroError> {
        let conn = self.connection()?;
        conn.execute(
            "INSERT INTO settings (key, value, updated_at)
             VALUES (?1, ?2, ?3)
             ON CONFLICT(key) DO UPDATE SET
               value = excluded.value,
               updated_at = excluded.updated_at",
            (key, value, chrono::Utc::now().timestamp()),
        )?;
        Ok(())
    }

    pub fn get_setting(&self, key: &str) -> Result<Option<String>, AeroError> {
        let conn = self.connection()?;
        let mut stmt = conn.prepare(
            "SELECT value FROM settings WHERE key = ?1"
        )?;
        let mut rows = stmt.query([key])?;
        if let Some(row) = rows.next()? {
            Ok(Some(row.get(0)?))
        } else {
            Ok(None)
        }
    }
}
```

- [ ] **Step 2: 创建 settings 命令**

```rust
// src-tauri/src/commands/settings.rs
use tauri::State;

use crate::AppState;
use crate::models::error::ErrorPayload;

#[tauri::command]
pub fn set_setting(
    key: String,
    value: String,
    state: State<'_, AppState>,
) -> Result<(), ErrorPayload> {
    state.db.set_setting(&key, &value).map_err(AeroError::to_payload)
}

#[tauri::command]
pub fn get_setting(
    key: String,
    state: State<'_, AppState>,
) -> Result<Option<String>, ErrorPayload> {
    state.db.get_setting(&key).map_err(AeroError::to_payload)
}
```

- [ ] **Step 3: 注册命令**

```rust
// src-tauri/src/commands/mod.rs
pub mod account;
pub mod settings;
```

```rust
// src-tauri/src/lib.rs
use commands::account::{add_account, delete_account, list_accounts, test_account_connection};
use commands::settings::{get_setting, set_setting};
// ...
.invoke_handler(tauri::generate_handler![
    greet,
    add_account,
    list_accounts,
    delete_account,
    test_account_connection,
    set_setting,
    get_setting,
])
```

- [ ] **Step 4: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/db/pool.rs src-tauri/src/commands/settings.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat(settings): add set_setting/get_setting IPC commands" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: 创建 useLocale 与初始化

**Files:**
- Create: `src/composables/useLocale.ts`
- Create: `src/stores/settings.ts`
- Modify: `src/App.vue`

**Interfaces:**
- Produces: `useLocale()` 提供 `locale`, `isReady`, `initLocale()`, `setLocale()`；`useSettingsStore` 提供 `set`/`get`。

- [ ] **Step 1: 创建 settings store**

```typescript
// src/stores/settings.ts
import { defineStore } from 'pinia';
import { invoke } from '@tauri-apps/api/core';

export const useSettingsStore = defineStore('settings', () => {
  async function set(key: string, value: string): Promise<void> {
    await invoke('set_setting', { key, value });
  }

  async function get(key: string): Promise<string | null> {
    return await invoke<string | null>('get_setting', { key });
  }

  return { set, get };
});
```

- [ ] **Step 2: 创建 useLocale**

```typescript
// src/composables/useLocale.ts
import { ref } from 'vue';
import { useI18n } from 'vue-i18n';
import type { Locale } from '@/i18n';
import { SUPPORTED_LOCALES, loadLocaleMessages } from '@/i18n';
import { useSettingsStore } from '@/stores/settings';

const SETTINGS_KEY = 'app.locale';

export function useLocale() {
  const { locale } = useI18n();
  const settings = useSettingsStore();
  const isReady = ref(false);

  async function initLocale() {
    const saved = await settings.get(SETTINGS_KEY);
    const target = resolveLocale(saved);
    await setLocale(target, false);
    isReady.value = true;
  }

  async function setLocale(value: Locale, persist = true) {
    if (!SUPPORTED_LOCALES.includes(value)) return;
    await loadLocaleMessages(value);
    locale.value = value;
    document.documentElement.setAttribute('lang', value);
    if (persist) {
      await settings.set(SETTINGS_KEY, value);
    }
  }

  function resolveLocale(raw: string | null): Locale {
    if (raw && SUPPORTED_LOCALES.includes(raw as Locale)) {
      return raw as Locale;
    }
    const osLang = navigator.language;
    if (osLang.startsWith('zh')) return 'zh-CN';
    return 'en';
  }

  return {
    locale,
    isReady,
    initLocale,
    setLocale,
    supportedLocales: SUPPORTED_LOCALES,
  };
}
```

- [ ] **Step 3: 在 App.vue 初始化语言**

```vue
<script setup lang="ts">
import { onMounted } from 'vue';
import { RouterView } from 'vue-router';
import CommandPalette from '@/components/CommandPalette.vue';
import ToastContainer from '@/components/ToastContainer.vue';
import { useLocale } from '@/composables/useLocale';

const { initLocale } = useLocale();

onMounted(() => {
  initLocale();
});
</script>

<template>
  <RouterView />
  <CommandPalette />
  <ToastContainer />
</template>
```

- [ ] **Step 4: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 5: Commit**

```bash
git add src/composables/useLocale.ts src/stores/settings.ts src/App.vue
git commit -m "feat(i18n): add useLocale composable and persist locale" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 5: 创建 Settings 页面与 LocaleSwitch

**Files:**
- Create: `src/components/LocaleSwitch.vue`
- Create: `src/views/SettingsView.vue`
- Modify: `src/router.ts`
- Modify: `src/components/AppSidebar.vue`

**Interfaces:**
- Produces: `LocaleSwitch` 组件，`/settings` 路由，Sidebar 设置入口。

- [ ] **Step 1: 创建 LocaleSwitch 组件**

```vue
<script setup lang="ts">
import { computed } from 'vue';
import { useLocale, type Locale } from '@/composables/useLocale';

const { locale, setLocale, supportedLocales } = useLocale();

const labels: Record<Locale, string> = {
  en: 'English',
  'zh-CN': '简体中文',
};

const selected = computed({
  get: () => locale.value as Locale,
  set: (value: Locale) => setLocale(value),
});
</script>

<template>
  <div class="flex items-center gap-3">
    <label class="text-sm text-text-secondary">{{ $t('settings.language') }}</label>
    <select
      v-model="selected"
      class="h-8 rounded-md border border-border bg-card px-2 text-sm text-text outline-none focus:border-primary"
    >
      <option v-for="loc in supportedLocales" :key="loc" :value="loc">
        {{ labels[loc] }}
      </option>
    </select>
  </div>
</template>
```

- [ ] **Step 2: 创建 SettingsView**

```vue
<script setup lang="ts">
import LocaleSwitch from '@/components/LocaleSwitch.vue';
</script>

<template>
  <div class="flex h-full flex-col overflow-y-auto bg-panel p-6">
    <h1 class="text-h1 mb-6 font-semibold text-text">{{ $t('nav.settings') }}</h1>
    <section class="rounded-lg border border-border bg-card p-4">
      <h2 class="mb-4 text-lg font-medium text-text">{{ $t('settings.language') }}</h2>
      <LocaleSwitch />
    </section>
  </div>
</template>
```

- [ ] **Step 3: 添加路由**

```typescript
// src/router.ts
import { createRouter, createWebHistory } from 'vue-router';
import InboxView from '@/views/InboxView.vue';
import AccountsView from '@/views/AccountsView.vue';
import SettingsView from '@/views/SettingsView.vue';

const routes = [
  { path: '/', component: InboxView },
  { path: '/accounts', component: AccountsView },
  { path: '/settings', component: SettingsView },
];

export default createRouter({
  history: createWebHistory(),
  routes,
});
```

- [ ] **Step 4: 在 Sidebar 添加入口**

在 `AppSidebar.vue` 的账户区下方或功能区新增设置链接。假设当前结构为静态列表，替换为：

```vue
<template>
  <aside class="flex h-full w-[240px] flex-col border-r border-border bg-panel">
    <nav class="flex flex-col gap-1 p-3">
      <RouterLink to="/" class="nav-item">{{ $t('nav.inbox') }}</RouterLink>
      <RouterLink to="/accounts" class="nav-item">{{ $t('nav.accounts') }}</RouterLink>
      <RouterLink to="/settings" class="nav-item">{{ $t('nav.settings') }}</RouterLink>
    </nav>
  </aside>
</template>
```

- [ ] **Step 5: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 6: Commit**

```bash
git add src/components/LocaleSwitch.vue src/views/SettingsView.vue src/router.ts src/components/AppSidebar.vue
git commit -m "feat(i18n): add Settings view and locale switcher" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 6: Status Bar 与 Command Palette 语言切换

**Files:**
- Modify: `src/components/StatusBar.vue`
- Modify: `src/components/CommandPalette.vue`
- Modify: `src/i18n/locales/en.json` 与 `src/i18n/locales/zh-CN.json`

**Interfaces:**
- Produces: Status Bar 显示当前语言并可切换；Command Palette 支持「切换语言」命令。

- [ ] **Step 1: 更新翻译文件**

在 `en.json` 和 `zh-CN.json` 的 `commandPalette` 与 `statusBar` 中补充：

```json
{
  "commandPalette": {
    "switchToEnglish": "Switch to English",
    "switchToChinese": "Switch to 简体中文"
  },
  "statusBar": {
    "language": "Language: {lang}"
  }
}
```

对应中文：

```json
{
  "commandPalette": {
    "switchToEnglish": "切换到 English",
    "switchToChinese": "切换到 简体中文"
  },
  "statusBar": {
    "language": "语言：{lang}"
  }
}
```

- [ ] **Step 2: 修改 StatusBar.vue**

在现有 StatusBar 右侧增加语言标签，点击弹出简单菜单切换：

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { useI18n } from 'vue-i18n';
import { useLocale, type Locale } from '@/composables/useLocale';

const { t } = useI18n();
const { locale, setLocale, supportedLocales } = useLocale();
const showLangMenu = ref(false);

const labels: Record<Locale, string> = {
  en: 'English',
  'zh-CN': '简体中文',
};

function selectLanguage(lang: Locale) {
  setLocale(lang);
  showLangMenu.value = false;
}
</script>

<template>
  <!-- 在现有 Status Bar 内容末尾追加 -->
  <div class="relative ml-auto">
    <button
      class="text-xs text-muted hover:text-text"
      @click="showLangMenu = !showLangMenu"
    >
      {{ t('statusBar.language', { lang: labels[locale as Locale] }) }}
    </button>
    <div
      v-if="showLangMenu"
      class="absolute bottom-full right-0 mb-1 rounded-lg border border-border bg-card py-1 shadow-modal"
    >
      <button
        v-for="loc in supportedLocales"
        :key="loc"
        class="block w-full px-4 py-1 text-left text-sm text-text hover:bg-panel"
        @click="selectLanguage(loc)"
      >
        {{ labels[loc] }}
      </button>
    </div>
  </div>
</template>
```

- [ ] **Step 3: 修改 CommandPalette.vue**

在结果列表中注册语言切换命令。假设当前 `CommandPalette` 使用静态 `results` 或已有搜索逻辑，在打开时注入两条固定命令：

```typescript
import { useLocale, type Locale } from '@/composables/useLocale';

const { setLocale } = useLocale();

const languageCommands = [
  {
    id: 'switch-lang-en',
    title: t('commandPalette.switchToEnglish'),
    action: () => setLocale('en'),
  },
  {
    id: 'switch-lang-zh',
    title: t('commandPalette.switchToChinese'),
    action: () => setLocale('zh-CN'),
  },
];
```

在搜索 query 为空或匹配到「语言」时展示这些命令；选择后执行 action 并关闭面板。

- [ ] **Step 4: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 5: Commit**

```bash
git add src/components/StatusBar.vue src/components/CommandPalette.vue src/i18n/locales/en.json src/i18n/locales/zh-CN.json
git commit -m "feat(i18n): add language switcher to status bar and command palette" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 7: 替换现有组件中的硬编码文案

**Files:**
- Modify: `src/components/AccountForm.vue`
- Modify: `src/components/AccountList.vue`
- Modify: `src/components/MailList.vue`
- Modify: `src/components/MailViewer.vue`
- Modify: `src/i18n/locales/en.json` 与 `src/i18n/locales/zh-CN.json`

**Interfaces:**
- Consumes: i18n keys 已定义。
- Produces: 所有用户可见静态文案走 `$t()` 或 `t()`。

- [ ] **Step 1: 扫描硬编码中文/英文**

```bash
grep -n -E "[一-龥]|>[A-Za-z ]+\s*<" src/components/*.vue | head -n 50
```

- [ ] **Step 2: 为每个组件抽取 key 并替换**

示例（AccountList.vue）：

```vue
<!-- 替换前 -->
<span>暂无账户</span>

<!-- 替换后 -->
<span>{{ $t('account.noAccounts') }}</span>
```

- [ ] **Step 3: 同步补充翻译文件**

每新增一个 key，同时在 `en.json` 和 `zh-CN.json` 中写入对应值。

- [ ] **Step 4: 验证**

```bash
pnpm lint
pnpm type-check
cargo check
```

- [ ] **Step 5: Commit**

```bash
git add src/components/*.vue src/i18n/locales/*.json
git commit -m "feat(i18n): localize all existing UI strings" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 8: 添加翻译 key 一致性检查

**Files:**
- Create: `scripts/check-i18n.ts`
- Modify: `package.json`

**Interfaces:**
- Produces: `pnpm i18n:check` 脚本，用于 CI 校验 en/zh-CN key 一致。

- [ ] **Step 1: 创建检查脚本**

```typescript
// scripts/check-i18n.ts
import en from '../src/i18n/locales/en.json';
import zh from '../src/i18n/locales/zh-CN.json';

function collectKeys(obj: unknown, prefix = ''): string[] {
  if (typeof obj !== 'object' || obj === null) return [];
  return Object.entries(obj).flatMap(([key, value]) => {
    const full = prefix ? `${prefix}.${key}` : key;
    if (typeof value === 'object' && value !== null) {
      return collectKeys(value, full);
    }
    return full;
  });
}

const enKeys = new Set(collectKeys(en));
const zhKeys = new Set(collectKeys(zh));

const missingInZh = [...enKeys].filter((k) => !zhKeys.has(k));
const missingInEn = [...zhKeys].filter((k) => !enKeys.has(k));

if (missingInZh.length || missingInEn.length) {
  console.error('i18n key mismatch:');
  missingInZh.forEach((k) => console.error(`  missing in zh-CN: ${k}`));
  missingInEn.forEach((k) => console.error(`  missing in en: ${k}`));
  process.exit(1);
}

console.log('i18n keys are consistent.');
```

- [ ] **Step 2: 添加 npm 脚本**

```json
{
  "scripts": {
    "i18n:check": "tsx scripts/check-i18n.ts"
  }
}
```

- [ ] **Step 3: 安装 tsx（如未安装）**

```bash
pnpm add -D tsx
```

- [ ] **Step 4: 运行检查**

```bash
pnpm i18n:check
```

- [ ] **Step 5: Commit**

```bash
git add scripts/check-i18n.ts package.json pnpm-lock.yaml
git commit -m "chore(i18n): add translation key consistency check" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 9: 最终验证与合并准备

**Files:**
- 全仓库

- [ ] **Step 1: 运行完整检查**

```bash
pnpm lint
pnpm type-check
pnpm i18n:check
cargo test
cargo check
cargo clippy -- -D warnings
cargo tauri build --no-bundle
```

- [ ] **Step 2: 启动应用手动验证**

```bash
cargo tauri dev
```

验证项：
- 启动后界面显示英文（默认）。
- Settings 页切换为简体中文后所有文案刷新。
- Status Bar 点击语言可切换。
- Command Palette 可执行语言切换命令。
- 关闭并重启应用，语言保持上次选择。
- 触发一个账户错误，确认提示为中文/英文本地化文本。

- [ ] **Step 3: 合并分支**

完成后使用 `superpowers:finishing-a-development-branch` 技能处理合并。

---

## Self-Review Checklist

- [x] Spec coverage：i18n 规范中的语言切换、持久化、错误码映射、多入口切换均有对应 Task。
- [x] Placeholder scan：无 TBD/TODO，所有步骤含代码与命令。
- [x] Type consistency：`Locale` 类型在 `src/i18n/index.ts` 与 `useLocale.ts` 中一致；`ErrorPayload` 在前后端定义一致。
- [x] Scope：本计划仅聚焦 i18n，不涉及翻译/AI 助手实现。
